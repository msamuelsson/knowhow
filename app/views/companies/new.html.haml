.header
 =render :partial => 'header_form'

%h2 Nuova Compagnia

= form_tag companies_path(@selected_area), :method => :post do
 = render :partial => 'company_form'

= button_to 'Annulla', companies_path(:area_filter => @selected_area), :method => :get
